# .render.yaml at the root of socapp

services:
  # === Backend (FastAPI) ===
  - type: web
    name: socapp-backend
    runtime: python
    plan: free
    rootDir: backendsoc          # Set working directory to backend
    buildCommand: pip install -r requirements.txt --only-binary=all
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
      - key: NEO4J_URI
        sync: false
      - key: NEO4J_USER
        sync: false
      - key: NEO4J_PASSWORD
        sync: false
      - key: NEO4J_DATABASE
        sync: false
      - key: AURA_INSTANCEID
        sync: false
      - key: AURA_INSTANCENAME
        sync: false
      - key: JWT_SECRET_KEY
        sync: false
      - key: JWT_ALGORITHM
        sync: false
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        sync: false

  # === Frontend (React / Vite) ===
  - type: web
    name: socapp-frontend
    runtime: node
    plan: free
    rootDir: hays                 # Set working directory to frontend
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist
    envVars:
      - key: VITE_API_URL
        sync: false
