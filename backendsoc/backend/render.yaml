services:
  - type: web
    name: college-social-backend
    runtime: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt --only-binary=all
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PIP_ONLY_BINARY
        value: "all"